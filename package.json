{
  "name": "proof",
  "version": "1.0.0",
  "description": "Hexagonal letter game",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "heroku-prebuild": "echo Node version: $(node --version) && echo npm version: $(npm --version)"
  },
  "dependencies": {
    "express": "^4.19.2",
    "ws": "^8.18.0",
    "pg": "^8.12.0",
    "uuid": "^9.0.0"
  },
  "engines": {
    "node": "22.x",
    "npm": "10.x"
  }
}